# 批次2详细信息导入说明

## 📋 概述

已生成完整的批次2（671-683）详细魔女信息导入脚本，包含所有13位魔女的完整档案数据。

## 📂 文件说明

### 主要文件
- **`batch2_detail_complete.sql`** - 完整的导入脚本（包含所有13位魔女）

### 脚本内容
- ✅ 671 小鸟游六花
- ✅ 672 富樫勇太
- ✅ 673 丹生谷森夏
- ✅ 674 五月七日茴香
- ✅ 675 凸守早苗
- ✅ 676 七宫智音
- ✅ 677 伊雷娜
- ✅ 678 维多利加
- ✅ 679 沙耶
- ✅ 680 芙兰
- ✅ 681 席拉
- ✅ 682 琪琪
- ✅ 683 冰上梅露露

## 🚀 执行方法

### 方法1：在SSMS中执行（推荐）

1. 打开 **SQL Server Management Studio (SSMS)**
2. 连接到数据库服务器（`localhost` 或你的服务器）
3. 打开 `batch2_detail_complete.sql` 文件
4. 确保当前数据库是 `WitchTrialWT`
5. 点击 **执行**（F5）或 **执行** 按钮
6. 查看执行结果和验证信息

### 方法2：使用sqlcmd命令行

```powershell
# 在项目根目录执行
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_detail_complete.sql -f 65001 -W
```

**参数说明：**
- `-S localhost` - 服务器名称
- `-d WitchTrialWT` - 数据库名称
- `-E` - 使用Windows身份验证
- `-i batch2_detail_complete.sql` - 输入SQL文件
- `-f 65001` - 使用UTF-8编码（重要！避免中文乱码）
- `-W` - 去除尾随空格

## 📊 数据内容

### 包含的字段

#### 基本信息
- 个人编号 (PersonalNo)
- 曾用名 (FormerName)
- 性别 (Gender)
- 出生日期 (BirthDate)
- 民族 (Ethnicity)
- 籍贯 (Birthplace)

#### 身体特征
- 身高 (Height)
- 体重 (Weight)
- 血型 (BloodType)

#### 联系方式
- 居住地址 (Address)
- 电话 (Phone)
- 邮箱 (Email)
- Line账号 (LineAccount)

#### 教育背景
- 最高学历 (HighestEducation)
- 教育经历 (EducationHistory) - JSON格式

#### 工作经历
- 工作经历 (WorkHistory) - JSON格式

#### 家庭关系
- 家庭结构 (FamilyStructure)
- 父亲信息 (Father)
- 母亲信息 (Mother)
- 其他家庭成员 (OtherFamily1/2/3)

#### 个性特征
- 技能特长 (Skills)
- 兴趣爱好 (Hobbies)
- 理想 (Dreams)
- 讨厌的事物 (Dislikes)
- 心理创伤 (Trauma)

#### 魔女相关
- 魔女化办法 (WitchTransformMethod)
- 备注 (Remarks)

## 🔍 数据格式说明

### JSON格式 - 教育经历

```json
[
    {
        "school": "学校名称",
        "degree": "学历类型（中学校/高等学校等）",
        "status": "状态（毕业/在读/退学）",
        "specialNote": "特殊说明"
    }
]
```

### JSON格式 - 工作经历

```json
[
    {
        "period": "时间范围（如：2018年至今）",
        "company": "公司名称",
        "position": "职位和职责",
        "salary": "薪资水平",
        "resignReason": "离职原因"
    }
]
```

## ⚠️ 注意事项

### 1. 数据完整性
- ✅ 所有必填字段都已填充
- ✅ 空值使用 `NULL` 或 `N'无'`
- ✅ 日期格式统一为 `YYYY-MM-DD`
- ✅ 所有中文字段使用 `N'中文'` 格式

### 2. 特殊处理
- **683号（冰上梅露露）**：某些字段为"不明"，已设为 `NULL`
- **工作经历**：无工作经历的魔女使用 `N'[]'`（空JSON数组）
- **教育经历**：根据实际情况填写，最少1条，最多3条

### 3. 编码问题
- ⚠️ **重要**：执行脚本时必须使用UTF-8编码（`-f 65001`），否则中文会乱码

### 4. 数据验证
脚本执行完成后会自动显示：
- 已更新的魔女数量
- 有个人编号的数量
- 有出生日期的数量
- 有教育经历的数量
- 所有批次2魔女的基本信息列表

## 🔄 执行前后验证

### 执行前检查
```sql
-- 检查批次2魔女是否存在
SELECT PrisonerNo, Name 
FROM wt.Witch 
WHERE BatchID = 2 
ORDER BY CAST(PrisonerNo AS INT);
```

### 执行后验证
```sql
-- 验证详细信息是否导入成功
SELECT 
    PrisonerNo AS [囚犯编号],
    Name AS [姓名],
    PersonalNo AS [个人编号],
    BirthDate AS [出生日期],
    HighestEducation AS [最高学历],
    Skills AS [技能特长]
FROM wt.Witch
WHERE BatchID = 2
ORDER BY CAST(PrisonerNo AS INT);
```

## 🐛 故障排除

### 问题1：中文乱码
**原因**：编码问题
**解决**：使用 `-f 65001` 参数，或在SSMS中确保文件编码为UTF-8

### 问题2：找不到魔女记录
**原因**：基本信息未导入
**解决**：先执行 `batch2_step2_import_witches_basic.sql`

### 问题3：JSON格式错误
**原因**：SQL字符串转义问题
**解决**：检查JSON中的引号是否已正确转义（使用 `\"` 或 `N'...'`）

### 问题4：日期格式错误
**原因**：日期字符串格式不正确
**解决**：确保所有日期格式为 `YYYY-MM-DD`

## ✅ 执行清单

执行前请确认：
- [ ] 数据库连接正常
- [ ] 当前数据库是 `WitchTrialWT`
- [ ] 批次2的基本信息已导入（671-683）
- [ ] SQL脚本文件编码为UTF-8

执行后请验证：
- [ ] 所有13位魔女的详细信息都已更新
- [ ] 个人编号、出生日期等关键字段不为空
- [ ] 教育经历JSON格式正确
- [ ] 工作经历JSON格式正确（如有）

## 📝 后续步骤

1. ✅ 执行导入脚本
2. ✅ 验证数据完整性
3. ⏳ 准备头像图片（671.png - 683.png）
4. ⏳ 在应用程序中测试显示效果

## 🎉 完成标志

当看到以下输出时，说明导入成功：

```
╔════════════════════════════════════════╗
║   ✅ 批次2详细信息导入完成！           ║
║   13位魔女的详细档案已全部更新         ║
╚════════════════════════════════════════╝
```

---

**生成时间**：2025-01-XX  
**数据来源**：批次2详细数据表格  
**脚本版本**：1.0


