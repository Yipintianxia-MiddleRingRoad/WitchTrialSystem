# 数据库结构说明

## 数据库概览

- **数据库名称**：`WitchTrialWT`
- **Schema**：`wt`
- **SQL Server 版本**：SQL Server 2022

---

## 数据表清单

| 序号 | 表名 | 说明 | 记录数（初始） |
|------|------|------|----------------|
| 1 | `wt.Role` | 角色表 | 4 |
| 2 | `wt.Island` | 岛屿表 | 1 |
| 3 | `wt.Batch` | 批次表 | 1 |
| 4 | `wt.[User]` | 用户表 | 16 |
| 5 | `wt.Witch` | 魔女表 | 13 |
| 6 | `wt.UserWitch` | 用户魔女关联表 | 15 |
| 7 | `wt.OperationLog` | 操作日志表 | 0 |
| 8 | `wt.GomokuMatchLog` | 五子棋对局日志表 | 0 |

---

## 详细表结构

### 1. wt.Role（角色表）

| 字段名 | 数据类型 | 主键 | 唯一 | 非空 | 说明 |
|--------|----------|------|------|------|------|
| **RoleID** | INT IDENTITY | ✅ | - | ✅ | 角色ID（自增） |
| Name | NVARCHAR(20) | - | ✅ | ✅ | 角色名称 |

**初始数据**：
- Admin（管理员）
- Meruru（梅露露）
- Warden（典狱长）
- Witch（魔女）

---

### 2. wt.Island（岛屿表）

| 字段名 | 数据类型 | 主键 | 唯一 | 非空 | 说明 |
|--------|----------|------|------|------|------|
| **IslandID** | INT IDENTITY | ✅ | - | ✅ | 岛屿ID（自增） |
| Name | NVARCHAR(50) | - | - | ✅ | 岛屿名称 |

**初始数据**：
- 魔女岛·壹

---

### 3. wt.Batch（批次表）

| 字段名 | 数据类型 | 主键 | 唯一 | 非空 | 默认值 | 说明 |
|--------|----------|------|------|------|--------|------|
| **BatchID** | INT IDENTITY | ✅ | - | ✅ | - | 批次ID（自增） |
| IslandID | INT | - | - | ✅ | - | 所属岛屿ID |
| WitchCount | INT | - | - | ✅ | 0 | 魔女数量（最多13人） |

**外键约束**：
- `FK_wt_Batch_Island`：IslandID → wt.Island(IslandID)

**业务规则**：
- 每个批次最多 13 位魔女
- 通过触发器 `wt.trg_Witch_BatchCount` 自动更新 WitchCount

---

### 4. wt.[User]（用户表）

| 字段名 | 数据类型 | 主键 | 唯一 | 非空 | 默认值 | 说明 |
|--------|----------|------|------|------|--------|------|
| **UserID** | INT IDENTITY | ✅ | - | ✅ | - | 用户ID（自增） |
| Username | NVARCHAR(50) | - | ✅ | ✅ | - | 用户名 |
| PasswordHash | NVARCHAR(64) | - | - | ✅ | - | 密码哈希值（SHA256） |
| Salt | NVARCHAR(64) | - | - | ✅ | - | 密码盐值 |
| RoleID | INT | - | - | ✅ | - | 角色ID |
| IslandID | INT | - | - | ❌ | NULL | 所属岛屿ID |
| BatchID | INT | - | - | ❌ | NULL | 所属批次ID |
| GomokuScore | INT | - | - | ✅ | 0 | 五子棋积分 |

**外键约束**：
- `FK_wt_User_Role`：RoleID → wt.Role(RoleID)
- `FK_wt_User_Island`：IslandID → wt.Island(IslandID)
- `FK_wt_User_Batch`：BatchID → wt.Batch(BatchID)

**初始账号**（密码均为 `123456`）：
- admin（管理员）
- meruru_regulator（梅露露）
- warden（典狱长）
- ema, hiro, anan, noah, leia, miria, margo, nanoka, alisa, sherry, hanna, coco, meruru（13位魔女）

---

### 5. wt.Witch（魔女表）

| 字段名 | 数据类型 | 主键 | 唯一 | 非空 | 默认值 | 说明 |
|--------|----------|------|------|------|--------|------|
| **WitchID** | INT IDENTITY | ✅ | - | ✅ | - | 魔女ID（自增） |
| Name | NVARCHAR(50) | - | - | ✅ | - | 魔女姓名 |
| Magic | NVARCHAR(100) | - | - | ❌ | NULL | 魔法能力 |
| PrisonerNo | NVARCHAR(20) | - | - | ❌ | NULL | 囚犯编号（658-670） |
| Status | NVARCHAR(20) | - | - | ✅ | 'Normal' | 状态 |
| ExecutionResult | NVARCHAR(50) | - | - | ❌ | NULL | 处刑结果 |
| AvatarPath | NVARCHAR(255) | - | - | ❌ | NULL | 头像路径 |
| IslandID | INT | - | - | ✅ | - | 所属岛屿ID |
| BatchID | INT | - | - | ✅ | - | 所属批次ID |
| DescriptionPublic | NVARCHAR(MAX) | - | - | ❌ | NULL | 公开描述 |

**外键约束**：
- `FK_wt_Witch_Island`：IslandID → wt.Island(IslandID)
- `FK_wt_Witch_Batch`：BatchID → wt.Batch(BatchID)

**初始数据**（13位魔女）：
| 编号 | 姓名 | 魔法 |
|------|------|------|
| 658 | 樱羽艾玛 | 魔女杀手 |
| 659 | 二阶堂希罗 | 死而复返 |
| 660 | 夏目安安 | 洗脑 |
| 661 | 城崎诺亚 | 液体操作 |
| 662 | 莲见蕾娅 | 视线诱导 |
| 663 | 佐伯米莉亚 | 身体互换 |
| 664 | 宝生玛格 | 声带模仿 |
| 665 | 黑部奈叶香 | 幻视 |
| 666 | 紫藤爱丽莎 | 发火 |
| 667 | 橘雪莉 | 怪力 |
| 668 | 远野汉娜 | 浮游 |
| 669 | 泽渡可可 | 千里眼 |
| 670 | 冰上梅露露 | 治疗 |

---

### 6. wt.UserWitch（用户魔女关联表）

| 字段名 | 数据类型 | 主键 | 唯一 | 非空 | 说明 |
|--------|----------|------|------|------|------|
| **UserID** | INT | ✅ | - | ✅ | 用户ID |
| WitchID | INT | - | - | ❌ | 魔女ID |

**外键约束**：
- `FK_UserWitch_User`：UserID → wt.[User](UserID) ON DELETE CASCADE
- `FK_UserWitch_Witch`：WitchID → wt.Witch(WitchID)

**说明**：
- 一对一关系：一个用户对应一个魔女档案
- warden 和 admin 的 WitchID 为 NULL
- meruru 和 meruru_regulator 都对应魔女 #670（冰上梅露露）

---

### 7. wt.OperationLog（操作日志表）

| 字段名 | 数据类型 | 主键 | 唯一 | 非空 | 默认值 | 说明 |
|--------|----------|------|------|------|--------|------|
| **LogID** | INT IDENTITY | ✅ | - | ✅ | - | 日志ID（自增） |
| UserID | INT | - | - | ❌ | NULL | 操作用户ID |
| Username | NVARCHAR(50) | - | - | ✅ | - | 操作用户名 |
| Action | NVARCHAR(50) | - | - | ✅ | - | 操作类型 |
| Target | NVARCHAR(100) | - | - | ❌ | NULL | 操作目标 |
| Detail | NVARCHAR(MAX) | - | - | ❌ | NULL | 操作详情 |
| CreatedAt | DATETIME2 | - | - | ✅ | SYSUTCDATETIME() | 创建时间（UTC） |

**说明**：
- 记录所有重要操作（登录、修改密码、更新魔女状态等）
- 使用存储过程 `wt.sp_LogOperation` 插入日志

---

### 8. wt.GomokuMatchLog（五子棋对局日志表）

| 字段名 | 数据类型 | 主键 | 唯一 | 非空 | 说明 |
|--------|----------|------|------|------|------|
| **MatchID** | INT IDENTITY | ✅ | - | ✅ | 对局ID（自增） |
| Player1Username | NVARCHAR(50) | - | - | ✅ | 玩家1用户名 |
| Player1Name | NVARCHAR(50) | - | - | ✅ | 玩家1显示名称 |
| Player2Username | NVARCHAR(50) | - | - | ✅ | 玩家2用户名 |
| Player2Name | NVARCHAR(50) | - | - | ✅ | 玩家2显示名称 |
| StartTime | DATETIME | - | - | ✅ | 对局开始时间 |
| EndTime | DATETIME | - | - | ✅ | 对局结束时间 |
| Player1Result | NVARCHAR(20) | - | - | ✅ | 玩家1结果（Win/Lose/Draw） |
| Player1ScoreChange | INT | - | - | ✅ | 玩家1分数变化 |
| Player2Result | NVARCHAR(20) | - | - | ✅ | 玩家2结果（Win/Lose/Draw） |
| Player2ScoreChange | INT | - | - | ✅ | 玩家2分数变化 |
| TotalMoves | INT | - | - | ✅ | 总步数 |
| Duration | INT | - | - | ✅ | 对局时长（秒） |

**说明**：
- 记录所有五子棋对局信息
- 积分规则：胜 +10，负 -5，和 0

---

## 存储过程

### 1. wt.sp_UpdateWitchStatus
更新魔女状态（带合法性验证）

**参数**：
- `@WitchID INT` - 魔女ID
- `@NewStatus NVARCHAR(20)` - 新状态
- `@ExecutionResult NVARCHAR(50)` - 处刑结果（可选）

### 2. wt.sp_LogOperation
记录操作日志

**参数**：
- `@UserID INT` - 用户ID（可选）
- `@Username NVARCHAR(50)` - 用户名
- `@Action NVARCHAR(50)` - 操作类型
- `@Target NVARCHAR(100)` - 操作目标（可选）
- `@Detail NVARCHAR(MAX)` - 操作详情（可选）

### 3. wt.sp_AddWitch
添加魔女（带批次人数限制）

**参数**：
- `@Name NVARCHAR(50)` - 魔女姓名
- `@Magic NVARCHAR(100)` - 魔法能力（可选）
- `@PrisonerNo NVARCHAR(20)` - 囚犯编号（可选）
- `@IslandID INT` - 岛屿ID
- `@BatchID INT` - 批次ID
- `@Status NVARCHAR(20)` - 状态（默认 'Normal'）
- `@AvatarPath NVARCHAR(255)` - 头像路径（可选）

---

## 触发器

### wt.trg_Witch_BatchCount
自动更新批次魔女数量

**触发时机**：INSERT, DELETE, UPDATE on wt.Witch

**功能**：自动更新 wt.Batch 表的 WitchCount 字段

---

## 视图

### wt.v_WitchPublic
魔女公开信息视图（用于普通魔女权限）

**字段**：WitchID, Name, Magic, PrisonerNo, Status, IslandID, BatchID, AvatarPath, DescriptionPublic

---

## 查询示例

### 查看所有表
```sql
SELECT TABLE_NAME 
FROM INFORMATION_SCHEMA.TABLES 
WHERE TABLE_SCHEMA = 'wt'
ORDER BY TABLE_NAME;
```

### 查看表结构
```sql
-- 查看 User 表结构
SELECT 
    COLUMN_NAME AS 字段名,
    DATA_TYPE AS 数据类型,
    CHARACTER_MAXIMUM_LENGTH AS 最大长度,
    IS_NULLABLE AS 可为空,
    COLUMN_DEFAULT AS 默认值
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_SCHEMA = 'wt' AND TABLE_NAME = 'User'
ORDER BY ORDINAL_POSITION;
```

### 查看主键
```sql
SELECT 
    tc.TABLE_NAME AS 表名,
    kcu.COLUMN_NAME AS 主键字段
FROM INFORMATION_SCHEMA.TABLE_CONSTRAINTS tc
JOIN INFORMATION_SCHEMA.KEY_COLUMN_USAGE kcu 
    ON tc.CONSTRAINT_NAME = kcu.CONSTRAINT_NAME
WHERE tc.TABLE_SCHEMA = 'wt' 
    AND tc.CONSTRAINT_TYPE = 'PRIMARY KEY'
ORDER BY tc.TABLE_NAME;
```

### 查看外键关系
```sql
SELECT 
    fk.name AS 外键名称,
    OBJECT_NAME(fk.parent_object_id) AS 从表,
    COL_NAME(fkc.parent_object_id, fkc.parent_column_id) AS 从表字段,
    OBJECT_NAME(fk.referenced_object_id) AS 到表,
    COL_NAME(fkc.referenced_object_id, fkc.referenced_column_id) AS 到表字段
FROM sys.foreign_keys fk
JOIN sys.foreign_key_columns fkc 
    ON fk.object_id = fkc.constraint_object_id
WHERE OBJECT_SCHEMA_NAME(fk.parent_object_id) = 'wt'
ORDER BY OBJECT_NAME(fk.parent_object_id);
```

### 查看所有用户及其角色
```sql
SELECT 
    u.UserID,
    u.Username,
    r.Name AS RoleName,
    w.Name AS WitchName,
    w.PrisonerNo,
    u.GomokuScore
FROM wt.[User] u
JOIN wt.Role r ON u.RoleID = r.RoleID
LEFT JOIN wt.UserWitch uw ON u.UserID = uw.UserID
LEFT JOIN wt.Witch w ON uw.WitchID = w.WitchID
ORDER BY u.UserID;
```

### 查看五子棋积分排行
```sql
SELECT 
    u.Username,
    w.Name AS WitchName,
    w.PrisonerNo,
    u.GomokuScore,
    RANK() OVER (ORDER BY u.GomokuScore DESC) AS Ranking
FROM wt.[User] u
LEFT JOIN wt.UserWitch uw ON u.UserID = uw.UserID
LEFT JOIN wt.Witch w ON uw.WitchID = w.WitchID
WHERE u.RoleID = 4  -- 只显示魔女
ORDER BY u.GomokuScore DESC;
```

---

## 数据库关系图

```
wt.Role (1) ──────< (N) wt.[User] (1) ────── (1) wt.UserWitch (1) ────── (1) wt.Witch
                            │                                                    │
wt.Island (1) ──────< (N) ──┤                                                    │
                            │                                                    │
wt.Batch (1) ───────< (N) ──┴────────────────────────────────────< (N) ─────────┘

wt.OperationLog (独立日志表)
wt.GomokuMatchLog (独立日志表)
```

---

## 注意事项

1. **密码安全**：所有密码使用 SHA256 + Salt 加密存储
2. **批次限制**：每个批次最多 13 位魔女，通过触发器强制执行
3. **日志记录**：重要操作自动记录到 OperationLog 表
4. **外键级联**：UserWitch 表设置了 ON DELETE CASCADE
5. **时间戳**：OperationLog 使用 UTC 时间（SYSUTCDATETIME()）
