# 五子棋功能部署说明

## 快速部署步骤

### 1. 更新数据库

在 SQL Server Management Studio 或命令行中执行：

```sql
USE WitchTrialWT;
GO

-- 添加五子棋积分字段
ALTER TABLE wt.[User]
ADD GomokuScore INT NOT NULL DEFAULT 0;
GO
```

或者直接执行脚本文件：

```bash
sqlcmd -S localhost -d WitchTrialWT -i add_gomoku_score_column.sql
```

### 2. 准备背景图

将以下两张背景图放到 `Images/ui/` 文件夹：

- `gomoku_mode_bg.png` - 五子棋模式选择界面背景（2560x1435）
- `gomoku_board_bg.png` - 五子棋棋盘界面背景（2403x1387）

**注意**：如果没有背景图，程序会使用纯色背景作为备用，不影响功能使用。

### 3. 编译运行

```bash
dotnet build
dotnet run
```

或在 Visual Studio 中按 F5 运行。

### 4. 测试功能

1. 以魔女账号登录（确保系统中至少有 2 个魔女账号）
2. 进入手机界面
3. 点击底部左下角红色图标
4. 选择"单设备对弈"
5. 选择对手并输入对手密码
6. 开始下棋！

## 文件清单

### 新增文件
```
UI/
  ├── GomokuBoardForm.cs      # 五子棋棋盘界面（核心逻辑）
  └── GomokuModeForm.cs       # 五子棋模式选择界面

DAL/
  └── Models/
      └── UserProfile.cs      # 用户档案模型

Images/ui/
  ├── gomoku_mode_bg.png      # 模式选择背景（需要准备）
  └── gomoku_board_bg.png     # 棋盘背景（需要准备）

add_gomoku_score_column.sql   # 数据库更新脚本
五子棋功能说明.md              # 功能详细说明
五子棋测试指南.md              # 测试指南
五子棋部署说明.md              # 本文件
```

### 修改文件
```
UI/PhoneForm.cs               # 添加五子棋入口按钮
DAL/UserProfileDAL.cs         # 添加获取和更新用户档案方法
README.md                     # 更新项目说明
CHANGELOG.md                  # 更新版本日志
```

## 数据库变更详情

### 新增字段

**表名**：`wt.[User]`

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| GomokuScore | INT | 0 | 五子棋积分 |

### 积分规则

- 初始积分：0
- 获胜：+10 分
- 失败：-5 分（最低为 0）
- 和棋：不计分

## 功能特性

### 已实现
✅ 单设备对弈（本地双人）  
✅ 对手选择与密码验证  
✅ 15x15 标准棋盘  
✅ 落子逻辑与交互  
✅ 五子连珠判定  
✅ 实时计时（步时 + 局时）  
✅ 积分系统  
✅ 悔棋功能（魔法按钮）  
✅ 和棋功能（伪证按钮）  

### 待实现
⏳ 多设备联机对弈  
⏳ 音效和动画  
⏳ 棋谱保存与复盘  
⏳ AI 对手  

## 常见问题

### Q: 数据库更新失败

A: 检查以下几点：
1. 确保连接到正确的数据库（WitchTrialWT）
2. 确保有足够的权限执行 ALTER TABLE
3. 如果字段已存在，脚本会自动跳过

### Q: 找不到对手

A: 确保系统中至少有 2 个魔女账号（RoleID = 2）

### Q: 密码验证失败

A: 确保输入的是对手的正确密码，密码区分大小写

### Q: 背景图不显示

A: 
1. 检查 `Images/ui/` 文件夹是否存在
2. 检查背景图文件名是否正确
3. 如果没有背景图，程序会使用纯色背景，不影响功能

### Q: 积分没有保存

A: 
1. 检查数据库连接是否正常
2. 检查 GomokuScore 字段是否已添加
3. 查看数据库日志是否有错误

## 验证部署

执行以下 SQL 查询验证部署是否成功：

```sql
-- 检查字段是否存在
SELECT COLUMN_NAME, DATA_TYPE, COLUMN_DEFAULT
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_SCHEMA = 'wt' 
  AND TABLE_NAME = 'User' 
  AND COLUMN_NAME = 'GomokuScore';

-- 查看所有魔女的积分
SELECT Username, Nickname, GomokuScore
FROM wt.[User]
WHERE RoleID = 2
ORDER BY GomokuScore DESC;
```

## 技术支持

如有问题，请查看：
- [五子棋功能说明.md](五子棋功能说明.md) - 功能详细说明
- [五子棋测试指南.md](五子棋测试指南.md) - 测试步骤
- [README.md](README.md) - 项目总体说明

## 版本信息

- 版本：V1.1.0
- 发布日期：2024-11-20
- 兼容性：.NET 9.0, SQL Server 2019+
