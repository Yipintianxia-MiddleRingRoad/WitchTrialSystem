# WitchTrialSystem 双岛屿扩展使用指南

## 🎯 版本V1.4.0 扩展内容

### 📍 新增结构
```
岛屿1 (魔女岛·壹)
├── 管理者: meruru_regulator (冰上梅露露)
├── 典狱长: warden (典狱长)
├── 批次1: 658-670 (13位魔女)
└── 批次2: 671-683 (13位魔女)

岛屿2 (魔女岛·贰)  
├── 管理者: utena_regulator (柊舞缇娜) [新增]
├── 典狱长: warden2 (典狱长2) [新增]
├── 批次3: 684-696 (13位魔女) [新增]
└── 批次4: 697-709 (13位魔女) [预留]
```

## 🔐 权限体系

### 角色权限矩阵
| 角色 | 管理范围 | 数据访问权限 | 备注 |
|------|----------|-------------|------|
| admin | 所有岛屿所有批次 | 全部数据 | 超级管理员 |
| meruru_regulator | 岛屿1所有批次 | 岛屿1数据 | 岛屿1管理者 |
| utena_regulator | 岛屿2所有批次 | 岛屿2数据 | 岛屿2管理者 |
| warden | 岛屿1所有批次 | 岛屿1数据 | 岛屿1典狱长 |
| warden2 | 岛屿2所有批次 | 岛屿2数据 | 岛屿2典狱长 |
| 658-670 | 岛屿1批次1 | 本批次13人 | 普通魔女 |
| 671-683 | 岛屿1批次2 | 本批次13人 | 普通魔女 |
| 684-696 | 岛屿2批次3 | 本批次13人 | 普通魔女 |
| 697-709 | 岛屿2批次4 | 本批次13人 | 预留魔女 |

### 特殊权限说明
- **五子棋系统**: 所有账号都可以跨岛屿自由对战
- **魔女视角**: 只能知道本岛屿本批次信息，不知道其他存在
- **管理者视角**: 可以看到本岛屿所有批次，但不能看到其他岛屿
- **典狱长视角**: 只能管理本岛屿，受本岛屿管理者控制

## 🚀 部署步骤

### 第1步: 执行数据库扩展
```sql
-- 1. 执行双岛屿扩展脚本
-- 文件: 双岛屿扩展_684_696.sql

-- 2. 设置新账号密码  
-- 文件: 设置新账号密码_684_696.sql
```

### 第2步: 导入魔女头像
将以下图片文件放入 `Images/` 目录:
```
684.png - 685.png - 686.png - 687.png
688.png - 689.png - 690.png - 691.png  
692.png - 693.png - 694.png - 695.png
696.png
```

### 第3步: 验证账号列表
| 账号类型 | 用户名 | 密码 | 权限 | 描述 |
|----------|--------|------|------|------|
| 超级管理员 | admin | 123456 | 全部 | 管理所有岛屿 |
| 岛屿1管理者 | meruru_regulator | 123456 | 岛屿1 | 管理岛屿1 |
| 岛屿2管理者 | utena_regulator | 123456 | 岛屿2 | 管理岛屿2 |
| 岛屿1典狱长 | warden | 123456 | 岛屿1 | 典狱长1 |
| 岛屿2典狱长 | warden2 | 123456 | 岛屿2 | 典狱长2 |
| 批次1魔女 | 658-670 | 123456 | 批次1 | 普通魔女 |
| 批次2魔女 | 671-683 | 123456 | 批次2 | 普通魔女 |
| 批次3魔女 | 684-696 | 123456 | 批次3 | 普通魔女 |

## 🎮 功能测试

### 1. 权限隔离测试
使用不同角色登录，验证数据访问范围:

```bash
# 测试岛屿1魔女 (例如: 658)
dotnet run
# 登录后应该只能看到658-670的其他12位魔女

# 测试岛屿2魔女 (例如: 684)  
dotnet run
# 登录后应该只能看到684-696的其他12位魔女

# 测试岛屿2管理者 (utena_regulator)
dotnet run
# 管理面板应该只能看到岛屿2的数据 (批次3和批次4)

# 测试超级管理员 (admin)
dotnet run  
# 管理面板应该可以看到所有岛屿所有批次数据
```

### 2. 五子棋跨岛屿测试
```bash
# 使用不同岛屿的魔女账号登录
# 验证可以跨岛屿匹配对战
# 例如: 658 vs 684, 671 vs 696
```

### 3. 权限边界测试
- 验证魔女无法看到其他岛屿信息
- 验证管理者无法看到其他岛屿信息  
- 验证典狱长受管理者控制
- 验证admin可以看到所有数据

## 🔧 技术实现

### 新增数据库表
```sql
-- 岛屿管理者表
CREATE TABLE wt.IslandRegulator(
    UserID INT PRIMARY KEY,
    IslandID INT NOT NULL,
    RegulatorName NVARCHAR(50) NOT NULL,
    FOREIGN KEY(UserID) REFERENCES wt.[User](UserID),
    FOREIGN KEY(IslandID) REFERENCES wt.Island(IslandID)
);

-- 岛屿典狱长表  
CREATE TABLE wt.IslandWarden(
    UserID INT PRIMARY KEY,
    IslandID INT NOT NULL,
    WardenName NVARCHAR(50) NOT NULL,
    FOREIGN KEY(UserID) REFERENCES wt.[User](UserID),
    FOREIGN KEY(IslandID) REFERENCES wt.Island(IslandID)
);
```

### 新增权限控制类
- `PermissionDAL.cs` - 根据角色动态构建SQL查询
- 支持Admin、Meruru、Warden、Witch四种角色
- 自动过滤岛屿和批次权限

### 管理界面更新
- `Form1.cs` - 添加权限过滤逻辑
- 管理者/典狱长只能看到本岛屿数据
- 魔女只能看到本批次数据

## 📝 下一步计划

### 批次4扩展 (697-709)
1. 等待697-709号魔女详细信息
2. 执行类似的导入脚本
3. 创建对应用户账号
4. 导入头像图片

### 可能的优化
1. **动态权限管理**: 运行时动态调整权限
2. **跨岛屿活动**: 允许特定跨岛屿事件
3. **权限继承**: 典狱长权限可由管理者动态调整
4. **审计日志**: 记录重要操作的审计信息

## ⚠️ 注意事项

1. **密码安全**: 所有新账号初始密码为123456，生产环境需要修改
2. **头像文件**: 确保684-696.png文件存在且可访问
3. **数据备份**: 执行扩展脚本前建议备份数据库
4. **权限测试**: 每个角色都要登录测试，确保权限隔离正确
5. **五子棋积分**: 跨岛屿对战会计入各自账号的积分

## 🐛 常见问题

### Q: 管理者如何管理典狱长？
A: 管理者可以在管理面板看到典狱长信息，但典狱长的数据访问范围仍受岛屿限制

### Q: 魔女是否可以跨岛屿五子棋对战？
A: 是的，五子棋系统支持跨岛屿自由匹配对战

### Q: 如何添加批次4的魔女？
A: 等待697-709号魔女数据，然后执行类似的导入脚本

### Q: 权限控制在哪里实现？
A: 主要在`PermissionDAL.cs`的`GetWitchesByPermission`方法中实现

---

*文档版本: V1.4.0*  
*最后更新: 2025-11-26*