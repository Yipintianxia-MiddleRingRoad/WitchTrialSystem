# 魔女详情窗口功能说明

## 📋 概述

实现了双击查看魔女详细档案的功能，通过构造函数传递参数（WitchID）来显示特定魔女的完整信息。

---

## 🎯 功能特性

### 1. 双击触发
- 在Form1的魔女列表中双击任意行
- 自动获取该行的WitchID
- 打开详情窗口显示完整档案

### 2. 详情窗口布局

#### 左侧面板（350px宽）
- **头像**：200x200px，支持占位图
- **核心信息卡片**：
  - 囚人番号、个人番号
  - 姓名、曾用名
  - 性别、年龄、出生日期
  - 民族、籍贯
- **身体特征卡片**：
  - 身高、体重、血型
- **状态信息卡片**：
  - 当前状态、魔法、处刑结果

#### 右侧面板（自动滚动）
- **联系方式**：地址、电话、邮箱、LINE账号
- **教育背景**：最高学历、教育经历（解析JSON）
- **工作经历**：工作经历详情（解析JSON）
- **家庭关系**：家庭结构、父母、其他成员
- **个性特征**：技能、爱好、理想、讨厌的事物
- **心理创伤**：创伤描述（特殊背景色）
- **魔女相关**：魔女化办法、公开描述
- **备注**：其他备注信息

---

## 🔧 技术实现

### 1. 构造函数传递参数

```csharp
public WitchDetailForm(int witchId)
{
    _witchId = witchId;
    InitializeForm();
    LoadData();
    BuildUI();
}
```

**说明**：
- 构造函数接收 `witchId` 参数
- 这就是"构造函数传递参数"的含义
- 通过这个ID从数据库加载对应魔女的完整信息

### 2. 数据加载

```csharp
private void LoadData()
{
    var dt = _dal.GetWitchDetail(_witchId);
    if (dt.Rows.Count > 0)
    {
        _witchData = dt.Rows[0];
        Text = $"魔女详细档案 - {GetString("Name")}";
    }
}
```

**WitchDAL.GetWitchDetail 方法**：
- 查询单个魔女的所有字段（共40个字段）
- 包括隐藏字段和JSON字段
- 返回DataTable供窗口使用

### 3. UI构建

#### 左侧面板
- 使用 `AddInfoCard` 方法创建信息卡片
- 卡片背景色：浅蓝色 `Color.FromArgb(240, 248, 255)`
- 自动计算高度，根据内容调整

#### 右侧面板
- 使用 `AddInfoSection` 方法创建信息区块
- 支持自定义背景色（心理创伤用红色）
- 自动滚动，支持长内容

### 4. JSON解析

```csharp
private string ParseEducationHistory()
{
    string json = GetString("EducationHistory");
    // 简单解析JSON格式的教育经历
    // 实际项目中应使用 System.Text.Json 或 Newtonsoft.Json
}
```

**当前实现**：
- 简单的字符串替换解析
- 适用于当前的JSON格式
- 后续可以升级为完整的JSON解析

---

## 📊 显示字段完整列表

### 核心识别信息（9个）
1. 囚人番号
2. 个人番号
3. 姓名
4. 曾用名
5. 性别
6. 年龄（计算）
7. 出生日期
8. 民族
9. 籍贯

### 身体特征（3个）
10. 身高
11. 体重
12. 血型

### 状态信息（3个）
13. 当前状态
14. 魔法
15. 处刑结果

### 联系方式（4个）
16. 地址
17. 电话
18. 邮箱
19. LINE账号

### 教育背景（2个）
20. 最高学历
21. 教育经历（JSON解析）

### 工作经历（1个）
22. 工作经历（JSON解析）

### 家庭关系（6个）
23. 家庭结构
24. 父亲
25. 母亲
26. 其他成员1
27. 其他成员2
28. 其他成员3

### 个性特征（4个）
29. 技能/特长
30. 兴趣爱好
31. 理想
32. 讨厌的事物

### 心理创伤（1个）
33. 创伤描述

### 魔女相关（2个）
34. 魔女化办法
35. 公开描述

### 其他（1个）
36. 备注

### 系统字段（不显示）
- WitchID
- IslandID
- BatchID
- AvatarPath（用于加载头像）

---

## 🎨 UI设计

### 颜色方案

| 元素 | 颜色 | 说明 |
|------|------|------|
| 标题栏 | #34495E | 深灰蓝色 |
| 背景 | #F5F5F5 | 浅灰色 |
| 卡片背景 | #F0F8FF | 浅蓝色 |
| 信息区背景 | #FAFAFA | 浅灰色 |
| 心理创伤背景 | #FFF0F0 | 浅红色 |
| 标题文字 | #34495E | 深灰蓝色 |
| 标签文字 | #646464 | 中灰色 |

### 字体

| 元素 | 字体 | 大小 | 样式 |
|------|------|------|------|
| 窗口标题 | 微软雅黑 | 16pt | Bold |
| 区块标题 | 微软雅黑 | 12pt | Bold |
| 卡片标题 | 微软雅黑 | 10pt | Bold |
| 标签 | 微软雅黑 | 9pt | Bold |
| 内容 | 微软雅黑 | 9pt | Regular |

### 尺寸

| 元素 | 尺寸 |
|------|------|
| 窗口 | 1200x800px |
| 左侧面板 | 350px宽 |
| 右侧面板 | 自动填充 |
| 头像 | 200x200px |
| 标题栏 | 60px高 |

---

## 🚀 使用方法

### 用户操作

1. 登录管理员账号（admin/meruru_regulator/warden）
2. 在主面板的魔女列表中找到目标魔女
3. **双击**该行的任意单元格
4. 自动打开详情窗口
5. 查看完整档案信息
6. 点击"关闭"按钮或按ESC键关闭窗口

### 开发者使用

```csharp
// 方式1：通过双击事件（已实现）
_grid.CellDoubleClick += Grid_CellDoubleClick;

// 方式2：手动打开（可用于其他场景）
int witchId = 658;  // 樱羽艾玛
using var detailForm = new WitchDetailForm(witchId);
detailForm.ShowDialog(this);
```

---

## 📝 代码文件

### 新增文件
- `UI/WitchDetailForm.cs` - 详情窗口实现

### 修改文件
- `Form1.cs` - 添加双击事件处理
- `DAL/WitchDAL.cs` - 添加GetWitchDetail方法

---

## 🔄 后续优化建议

### 1. JSON解析优化
```csharp
// 当前：简单字符串替换
// 建议：使用 System.Text.Json
using System.Text.Json;

var educationList = JsonSerializer.Deserialize<List<Education>>(json);
```

### 2. 编辑功能
- 添加"编辑"按钮
- 允许修改魔女信息
- 保存到数据库

### 3. 打印/导出功能
- 导出为PDF
- 打印档案
- 导出为Word文档

### 4. 图片管理
- 上传/更换头像
- 图片预览
- 图片裁剪

### 5. 历史记录
- 显示修改历史
- 审计日志
- 版本对比

---

## 🐛 注意事项

### 1. NULL值处理
- 所有字段都做了NULL检查
- 空值显示为空白，不会报错
- "无"字符串会被过滤不显示

### 2. 图片加载
- 支持相对路径和绝对路径
- 找不到图片时使用占位图
- 图片加载失败不会影响窗口显示

### 3. 长文本处理
- 使用MaximumSize限制宽度
- 自动换行
- 右侧面板支持滚动

### 4. 性能考虑
- 每次打开窗口都重新查询数据库
- 确保数据是最新的
- 关闭窗口时释放资源

---

## 📊 测试建议

### 测试用例

1. **正常流程**：
   - 双击任意魔女行
   - 验证所有字段正确显示
   - 验证头像正确加载

2. **边界情况**：
   - 双击列头（应该被忽略）
   - 双击空白区域
   - 数据库中没有该魔女

3. **数据完整性**：
   - 测试有完整数据的魔女（如658）
   - 测试部分字段为空的魔女
   - 测试JSON字段解析

4. **UI响应**：
   - 窗口大小调整
   - 滚动条功能
   - 关闭按钮

---

## 📞 技术支持

如有问题，请查看：
- `UI/WitchDetailForm.cs` 源代码
- `Form1.cs` 双击事件处理
- `DAL/WitchDAL.cs` 数据访问方法

---

**功能已完成！** 🎉

现在可以在梅露露管理端双击任意魔女查看完整档案了！
