# 批次2导入编码问题修复说明

## 🔍 问题诊断

### 问题表现
- SQL脚本执行后，中文数据出现乱码（如 `ѽݿĸΪ`）
- 导入日志文件显示中文字符无法正确显示
- 数据库中存储的中文数据可能已损坏

### 根本原因
1. **编码不匹配**：SQL脚本文件可能是UTF-8编码，但`sqlcmd`默认使用系统代码页（Windows通常是GBK/GB2312）
2. **代码页未指定**：执行SQL脚本时未指定UTF-8代码页（65001）
3. **文件编码问题**：部分SQL文件可能不是UTF-8编码保存

## ✅ 解决方案

### 方案1：使用UTF-8代码页执行（推荐）

在执行任何包含中文的SQL脚本时，添加 `-f 65001` 参数：

```powershell
# 单个脚本执行
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_simple_import.sql -f 65001

# 完整导入流程
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_import_all.sql -f 65001
```

**参数说明：**
- `-f 65001`：指定输入文件代码页为UTF-8（65001是UTF-8的代码页编号）
- `-C`：也可以使用此参数，让sqlcmd信任服务器证书

### 方案2：使用PowerShell导入脚本（最可靠）

已创建 `batch2_import_utf8.ps1` 脚本，自动处理所有编码问题：

```powershell
# 执行导入
.\batch2_import_utf8.ps1
```

**优势：**
- ✅ 自动设置UTF-8编码
- ✅ 逐步执行，便于排查问题
- ✅ 详细的执行日志和错误提示
- ✅ 自动验证导入结果

### 方案3：转换SQL文件编码

如果SQL文件不是UTF-8编码，需要先转换：

```powershell
# 使用PowerShell转换文件编码
Get-Content "batch2_simple_import.sql" -Encoding Default | 
    Set-Content "batch2_simple_import_utf8.sql" -Encoding UTF8
```

## 📋 修复步骤

### 步骤1：清理已损坏的数据（如果存在）

```sql
-- 删除批次2的所有数据（如果需要重新导入）
USE WitchTrialWT;
GO

DELETE FROM wt.UserWitch WHERE UserID IN (SELECT UserID FROM wt.[User] WHERE BatchID = 2);
DELETE FROM wt.[User] WHERE BatchID = 2;
DELETE FROM wt.Witch WHERE BatchID = 2;
DELETE FROM wt.Batch WHERE BatchID = 2;
GO
```

### 步骤2：使用UTF-8编码重新导入

**方式A：使用PowerShell脚本（推荐）**
```powershell
.\batch2_import_utf8.ps1
```

**方式B：手动执行SQL脚本**
```powershell
# 步骤1：创建批次
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_step1_create_batch.sql -f 65001

# 步骤2：导入基本信息
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_step2_import_witches_basic.sql -f 65001

# 步骤3：导入详细信息（分3部分）
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_step3_import_details_part1.sql -f 65001
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_step3_import_details_part2.sql -f 65001
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_step3_import_details_part3.sql -f 65001

# 步骤4：创建用户
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_step4_create_users.sql -f 65001
```

### 步骤3：验证导入结果

```sql
USE WitchTrialWT;
GO

-- 检查魔女数据（应该显示正确的中文）
SELECT PrisonerNo, Name, Magic, BatchID 
FROM wt.Witch 
WHERE BatchID = 2 
ORDER BY PrisonerNo;

-- 检查用户数据
SELECT Username, BatchID 
FROM wt.[User] 
WHERE BatchID = 2 
ORDER BY Username;

-- 检查关联
SELECT 
    u.Username,
    w.Name,
    w.PrisonerNo
FROM wt.[User] u
JOIN wt.UserWitch uw ON u.UserID = uw.UserID
JOIN wt.Witch w ON uw.WitchID = w.WitchID
WHERE u.BatchID = 2
ORDER BY u.Username;
```

## 🔧 预防措施

### 1. 统一使用UTF-8编码

- ✅ SQL脚本文件统一保存为 **UTF-8 with BOM** 或 **UTF-8**
- ✅ 在SQL Server Management Studio (SSMS)中：文件 → 高级保存选项 → 选择"Unicode (UTF-8 with signature) - Codepage 65001"

### 2. 统一执行方式

创建统一的执行脚本，始终使用UTF-8代码页：

```powershell
# 创建执行函数
function Invoke-SqlScript {
    param(
        [string]$ScriptPath,
        [string]$Server = "localhost",
        [string]$Database = "WitchTrialWT"
    )
    
    Write-Host "执行: $ScriptPath" -ForegroundColor Cyan
    sqlcmd -S $Server -d $Database -E -i $ScriptPath -f 65001 -W
    if ($LASTEXITCODE -ne 0) {
        Write-Host "错误: 脚本执行失败" -ForegroundColor Red
        return $false
    }
    return $true
}

# 使用示例
Invoke-SqlScript -ScriptPath "batch2_step1_create_batch.sql"
```

### 3. 数据库列设置

确保数据库列使用支持Unicode的类型：
- ✅ 使用 `NVARCHAR` 而不是 `VARCHAR`
- ✅ 使用 `NCHAR` 而不是 `CHAR`
- ✅ 字符串常量使用 `N'中文'` 前缀

## 📝 验证清单

导入完成后，请验证以下项目：

- [ ] 批次2创建成功（BatchID = 2）
- [ ] 13位魔女基本信息导入成功（671-683）
- [ ] 魔女名称显示正确的中文（无乱码）
- [ ] 魔法名称显示正确的中文（无乱码）
- [ ] 13个用户账号创建成功（671-683）
- [ ] 用户-魔女关联建立成功（13条记录）
- [ ] 详细信息导入成功（如果执行了步骤3）

## 🚨 常见问题

### Q1: 为什么单条INSERT可以，批量导入就乱码？
**A:** 单条执行时可能使用的是SSMS，它自动处理了编码。批量执行sqlcmd时如果未指定代码页，就会使用系统默认编码，导致UTF-8文件被错误解析。

### Q2: 已经导入的数据出现乱码怎么办？
**A:** 需要删除乱码数据后重新导入。使用方案2的PowerShell脚本可以自动清理并重新导入。

### Q3: 如何检查SQL文件编码？
**A:** 
- 在VS Code中，右下角显示文件编码
- 使用PowerShell: `Get-Content "file.sql" -Encoding Byte | Select-Object -First 3`，查看BOM标识

### Q4: 能否一次性修复所有SQL脚本？
**A:** 可以，创建批量转换脚本：

```powershell
Get-ChildItem -Path "." -Filter "*.sql" | ForEach-Object {
    $content = Get-Content $_.FullName -Raw -Encoding Default
    $content | Set-Content $_.FullName -Encoding UTF8 -NoNewline
    Write-Host "转换: $($_.Name)"
}
```

## ✅ 总结

**关键要点：**
1. ✅ 始终使用 `-f 65001` 参数执行包含中文的SQL脚本
2. ✅ SQL文件统一保存为UTF-8编码
3. ✅ 使用PowerShell脚本可以更好地控制编码
4. ✅ 导入前先清理已损坏的数据

**推荐工作流程：**
```
1. 清理旧数据
   ↓
2. 使用 batch2_import_utf8.ps1 执行导入
   ↓
3. 验证导入结果
   ↓
4. 执行详细信息导入（如需要）
```

如有问题，请检查执行日志和数据库中的实际数据。

