# 🚀 快速开始指南

## 给项目负责人（第一次上传到 GitHub）

### 1. 创建 GitHub 仓库

1. 登录 GitHub
2. 点击右上角 "+" → "New repository"
3. 填写仓库名：`WitchTrialSystem`
4. 选择 Private（私有）
5. **不要**勾选任何初始化选项
6. 点击 "Create repository"

### 2. 上传项目

在项目文件夹中打开命令行（PowerShell 或 CMD），执行：

```bash
git init
git add .
git commit -m "初始提交：魔女审判系统"
git remote add origin https://github.com/Yipintianxia-MiddleRingRoad/WitchTrialSystem.git
git branch -M main
git push -u origin main
```

### 3. 邀请小组成员

1. 在 GitHub 仓库页面，点击 "Settings"
2. 点击 "Collaborators"
3. 点击 "Add people"
4. 输入成员的 GitHub 用户名

### 4. 分享仓库链接

把仓库链接发给小组成员：
```
https://github.com/你的用户名/WitchTrialSystem
```

---

## 给小组成员（克隆项目）

### 1. 安装必要软件

- Git：https://git-scm.com/downloads
- .NET 9.0 SDK：https://dotnet.microsoft.com/download
- SQL Server（任意版本）
- Visual Studio 2022 或 VS Code（可选）

### 2. 克隆项目

```bash
git clone https://github.com/项目负责人/WitchTrialSystem.git
cd WitchTrialSystem
```

### 3. 配置数据库连接

```bash
# Windows CMD
copy appsettings.example.json appsettings.json

# 或 PowerShell
Copy-Item appsettings.example.json appsettings.json
```

然后编辑 `appsettings.json`：

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=.;Database=WitchTrialWT;Trusted_Connection=True;TrustServerCertificate=True;"
  }
}
```

**说明**：
- `Server=.` 表示本地默认实例
- 如果用 SQLEXPRESS：`Server=.\\SQLEXPRESS`
- 如果用 SQL 登录：`Server=.;Database=WitchTrialWT;User ID=sa;Password=你的密码;TrustServerCertificate=True;`

### 4. 创建数据库

在 SQL Server Management Studio (SSMS) 中：

1. 连接到你的 SQL Server
2. 执行 `Data/create_tables.sql`（如果有）
3. 执行 `Data/init_data.sql`（如果有）

或者手动创建：

```sql
CREATE DATABASE WitchTrialWT;
GO

USE WitchTrialWT;
GO

CREATE SCHEMA wt;
GO

-- 然后执行其他建表脚本
```

### 5. 运行项目

```bash
dotnet run
```

或在 Visual Studio 中按 F5。

---

## 日常开发流程

### 每天开始工作前

```bash
# 拉取最新代码
git pull
```

### 完成工作后

```bash
# 查看修改了什么
git status

# 添加所有修改
git add .

# 提交（写清楚你做了什么）
git commit -m "新增：手机界面功能"

# 推送到 GitHub
git push
```

---

## 常见问题速查

### ❌ 提示"未找到配置文件"

```bash
copy appsettings.example.json appsettings.json
```

### ❌ 提示"SQL连接失败"

1. 检查 SQL Server 服务是否启动
2. 检查 `appsettings.json` 中的连接字符串
3. 确认数据库 `WitchTrialWT` 已创建

### ❌ 提示"Permission denied"（Git）

你没有权限，请联系项目负责人邀请你。

### ❌ 图片不显示

确保 `Images/` 文件夹中有对应的图片文件。

### ❌ 代码冲突

```bash
git pull
# 手动解决冲突（编辑冲突的文件）
git add .
git commit -m "解决冲突"
git push
```

---

## 提交信息规范

使用清晰的提交信息：

- `新增：添加手机界面`
- `修复：登录界面输入框位置错误`
- `优化：改进数据库连接逻辑`
- `文档：更新 README`

---

## 需要更多帮助？

查看详细文档：

- 📖 `README.md` - 项目说明
- 📖 `GIT使用指南.md` - Git 详细教程
- 📖 `打包发布指南.md` - 如何打包发布
- 📖 `项目协作总结.md` - 协作规范
- 📖 `改进完成清单.md` - 所有改进内容

---

## 🎉 开始开发吧！

记住三个原则：

1. **每天先 `git pull`**
2. **经常提交，写清楚提交信息**
3. **遇到问题及时沟通**

祝开发顺利！ 🚀
