# 魔女表扩展字段说明

## 概述

为了支持更详细的魔女档案管理，对 `wt.Witch` 表进行了扩展，新增了 28 个字段。

---

## 新增字段列表

### 1️⃣ 基本身份信息（6个字段）

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|----------|------|------|
| PersonalNo | NVARCHAR(20) | 个人番号 | 1234-5678-9011 |
| FormerName | NVARCHAR(100) | 曾用名 | 无 / 田中花子 |
| Gender | NVARCHAR(10) | 性别 | 女 / 男 |
| BirthDate | DATE | 出生日期 | 2010-03-05 |
| Ethnicity | NVARCHAR(50) | 民族 | 大和民族 |
| Birthplace | NVARCHAR(100) | 籍贯 | 东京都 |

### 2️⃣ 身体特征（3个字段）

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|----------|------|------|
| Height | DECIMAL(5,2) | 身高（厘米） | 156.00 |
| Weight | DECIMAL(5,2) | 体重（公斤） | 48.00 |
| BloodType | NVARCHAR(10) | 血型 | A / B / O / AB |

### 3️⃣ 联系方式（4个字段）

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|----------|------|------|
| Address | NVARCHAR(500) | 住民票地址 | 东京都涩谷区道玄坂 2 丁目 |
| Phone | NVARCHAR(50) | 电话 | 03-1234-5678 |
| Email | NVARCHAR(100) | 邮箱 | sakuraba_ema@yahoo.co.jp |
| LineAccount | NVARCHAR(100) | LINE账号 | ema_sakura0305 |

### 4️⃣ 教育背景（2个字段）

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|----------|------|------|
| HighestEducation | NVARCHAR(100) | 最高学历 | 中学校毕业 |
| EducationHistory | NVARCHAR(MAX) | 教育经历（JSON格式） | 见下方JSON示例 |

**EducationHistory JSON 格式**：
```json
[
  {
    "school": "东京都立樱丘中学校",
    "degree": "中学校",
    "status": "毕业",
    "specialNote": "初中时旁观好友月代雪霸凌致其自杀"
  },
  {
    "school": "东京都立樱丘高等学校",
    "degree": "高等学校",
    "status": "未入学",
    "specialNote": "高中开学前一日被抓至魔女岛"
  }
]
```

### 5️⃣ 工作经历（1个字段）

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|----------|------|------|
| WorkHistory | NVARCHAR(MAX) | 工作经历（JSON格式） | 见下方JSON示例 |

**WorkHistory JSON 格式**：
```json
[
  {
    "period": "2020/04-2022/03",
    "company": "东京商事株式会社",
    "position": "营业部助理",
    "salary": "月薪 25 万日元",
    "resignReason": "被发现魔女身份"
  }
]
```

如果没有工作经历，填写：`[]`

### 6️⃣ 家庭关系（6个字段）

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|----------|------|------|
| FamilyStructure | NVARCHAR(200) | 家庭基本情况 | 核心成员为父母 |
| Father | NVARCHAR(200) | 父亲信息 | 樱羽健一，45 岁，会社社员，东京商事株式会社 |
| Mother | NVARCHAR(200) | 母亲信息 | 樱羽静香，43 岁，家庭主妇 |
| OtherFamily1 | NVARCHAR(200) | 其他家庭成员1 | 哥哥：樱羽太郎，20 岁，大学生 |
| OtherFamily2 | NVARCHAR(200) | 其他家庭成员2 | NULL |
| OtherFamily3 | NVARCHAR(200) | 其他家庭成员3 | NULL |

### 7️⃣ 个性特征（5个字段）

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|----------|------|------|
| Skills | NVARCHAR(500) | 技能/特长 | 推理能力敏锐、观察力强、吃饭快速 |
| Hobbies | NVARCHAR(500) | 兴趣爱好 | 寻找美食店、和朋友相处 |
| Dreams | NVARCHAR(500) | 理想 | 交 100 个朋友 |
| Dislikes | NVARCHAR(500) | 讨厌的事物 | 孤独、被排挤 |
| Trauma | NVARCHAR(MAX) | 心理创伤 | 旁观好友月代雪霸凌致其自杀，篡改记忆掩盖创伤 |

### 8️⃣ 魔女相关（1个字段）

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|----------|------|------|
| WitchTransformMethod | NVARCHAR(500) | 魔女化办法 | 无 / 自然觉醒 / 药物诱发 |

### 9️⃣ 其他（1个字段）

| 字段名 | 数据类型 | 说明 | 示例 |
|--------|----------|------|------|
| Remarks | NVARCHAR(MAX) | 备注 | 其他需要记录的信息 |

---

## 原有字段（保持不变）

| 字段名 | 数据类型 | 说明 |
|--------|----------|------|
| WitchID | INT IDENTITY | 魔女ID（主键） |
| Name | NVARCHAR(50) | 姓名 |
| Magic | NVARCHAR(100) | 魔法能力 |
| PrisonerNo | NVARCHAR(20) | 囚人番号 |
| Status | NVARCHAR(20) | 状态 |
| ExecutionResult | NVARCHAR(50) | 处刑结果 |
| AvatarPath | NVARCHAR(255) | 头像路径 |
| IslandID | INT | 岛屿ID |
| BatchID | INT | 批次ID |
| DescriptionPublic | NVARCHAR(MAX) | 公开描述 |

---

## 使用步骤

### 步骤 1：执行扩展脚本

在 SSMS 中执行：
```sql
-- 执行此文件添加新字段
add_witch_extended_fields.sql
```

### 步骤 2：导入数据

使用以下三种方法之一：

**方法 A：单个更新**
```sql
-- 适合少量数据，逐个魔女更新
-- 参考：魔女详细档案导入模板.sql - 方法1
```

**方法 B：批量导入**
```sql
-- 适合大量数据，使用临时表批量更新
-- 参考：魔女详细档案导入模板.sql - 方法2
```

**方法 C：Excel 导入**
1. 导出当前数据到 Excel
2. 在 Excel 中编辑
3. 使用 SSMS 的导入向导导入

### 步骤 3：验证数据

```sql
-- 查看所有魔女的基本信息
SELECT * FROM wt.v_WitchFullProfile;

-- 查看特定魔女的完整档案
SELECT * FROM wt.v_WitchFullProfile WHERE 囚人番号 = '658';
```

---

## 新增视图

### wt.v_WitchFullProfile

完整的魔女档案视图，包含所有字段和计算字段（如年龄）。

**使用示例**：
```sql
-- 查询所有魔女的完整档案
SELECT * FROM wt.v_WitchFullProfile;

-- 查询特定年龄段的魔女
SELECT 姓名, 年龄, 魔法, 状态 
FROM wt.v_WitchFullProfile 
WHERE 年龄 BETWEEN 15 AND 18;

-- 查询有心理创伤的魔女
SELECT 姓名, 心理创伤 
FROM wt.v_WitchFullProfile 
WHERE 心理创伤 IS NOT NULL;
```

---

## 数据填写规范

### 必填字段（建议）
- PersonalNo（个人番号）
- Gender（性别）
- BirthDate（出生日期）
- Height（身高）
- Weight（体重）
- BloodType（血型）
- HighestEducation（最高学历）

### 可选字段
- 其他所有字段都是可选的
- 如果暂时没有数据，可以填 NULL 或留空

### 特殊字段说明

**EducationHistory 和 WorkHistory**：
- 必须使用 JSON 格式
- 如果没有记录，填写 `[]`
- 如果有多条记录，在数组中添加多个对象

**日期格式**：
- BirthDate：`YYYY-MM-DD`（如：2010-03-05）

**数值格式**：
- Height：保留两位小数（如：156.00）
- Weight：保留两位小数（如：48.00）

---

## 示例数据：樱羽艾玛

```sql
UPDATE wt.Witch
SET 
    PersonalNo = '1234-5678-9011',
    FormerName = '无',
    Gender = '女',
    BirthDate = '2010-03-05',
    Ethnicity = '大和民族',
    Birthplace = '东京都',
    Height = 156.00,
    Weight = 48.00,
    BloodType = 'A',
    Address = '东京都涩谷区道玄坂 2 丁目',
    Phone = '03-1234-5678',
    Email = 'sakuraba_ema@yahoo.co.jp',
    LineAccount = 'ema_sakura0305',
    HighestEducation = '中学校毕业',
    EducationHistory = N'[...]',  -- 见完整脚本
    WorkHistory = N'[]',
    FamilyStructure = '核心成员为父母',
    Father = '樱羽健一，45 岁，会社社员，东京商事株式会社',
    Mother = '樱羽静香，43 岁，家庭主妇',
    Skills = '推理能力敏锐、观察力强、吃饭快速',
    Hobbies = '寻找美食店、和朋友相处',
    Dreams = '交 100 个朋友',
    Dislikes = '孤独、被排挤',
    Trauma = '旁观好友月代雪霸凌致其自杀，篡改记忆掩盖创伤',
    WitchTransformMethod = '无',
    Remarks = '无'
WHERE PrisonerNo = '658';
```

---

## 注意事项

1. **数据隐私**：这些字段包含敏感个人信息，注意权限控制
2. **JSON 格式**：教育和工作经历使用 JSON 格式，便于扩展和查询
3. **字段长度**：注意不要超过字段的最大长度限制
4. **NULL 值**：所有新增字段都允许 NULL，可以逐步完善数据
5. **备份**：在批量更新前，建议先备份数据库

---

## 后续开发建议

### 阶段 1：数据录入（当前）
- ✅ 扩展数据库表结构
- ⏳ 录入所有魔女的详细档案

### 阶段 2：界面开发（未来）
- 创建魔女详细档案查看界面
- 创建魔女档案编辑界面
- 添加搜索和筛选功能

### 阶段 3：权限控制（未来）
- 不同角色查看不同级别的信息
- 普通魔女只能查看公开信息
- 管理员可以查看完整档案

---

## 相关文件

- `add_witch_extended_fields.sql` - 字段扩展脚本
- `魔女详细档案导入模板.sql` - 数据导入模板
- `数据库结构说明.md` - 完整数据库文档
- `数据库结构查询命令.sql` - 实用查询命令

---

## 技术支持

如有问题，请查看：
1. 数据库结构说明文档
2. SQL 脚本中的注释
3. 示例数据（樱羽艾玛）
