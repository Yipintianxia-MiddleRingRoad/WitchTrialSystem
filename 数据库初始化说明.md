# 数据库初始化说明

## 数据库要求

- SQL Server 2019 或更高版本
- 数据库名称：`WitchTrialWT`
- 架构：`wt`

## 初始化步骤

### 1. 创建数据库

在 SQL Server Management Studio (SSMS) 中执行：

```sql
CREATE DATABASE WitchTrialWT;
GO

USE WitchTrialWT;
GO

-- 创建架构
CREATE SCHEMA wt;
GO
```

### 2. 创建表结构

请执行项目中的建表脚本（如果有 `create_tables.sql` 文件）。

主要表结构应包括：

- `wt.[User]`：用户表
- `wt.Witch`：魔女信息表
- `wt.Island`：岛屿表
- `wt.Batch`：批次表
- `wt.UserProfile`：用户档案表（视图或表）
- `wt.AuditLog`：审计日志表

### 3. 初始化数据

执行初始化数据脚本（如果有 `init_data.sql` 文件）。

### 4. 验证数据库

执行以下查询验证数据库是否正确创建：

```sql
-- 检查表是否存在
SELECT TABLE_SCHEMA, TABLE_NAME 
FROM INFORMATION_SCHEMA.TABLES 
WHERE TABLE_SCHEMA = 'wt'
ORDER BY TABLE_NAME;

-- 检查用户数据
SELECT * FROM wt.[User];

-- 检查魔女数据
SELECT * FROM wt.Witch;
```

## 常见问题

### Q: 提示"无法打开数据库"

A: 确保数据库名称为 `WitchTrialWT`（区分大小写）。

### Q: 提示"架构不存在"

A: 执行 `CREATE SCHEMA wt;` 创建架构。

### Q: 如何重置数据库

A: 执行以下命令删除并重新创建：

```sql
USE master;
GO

-- 断开所有连接
ALTER DATABASE WitchTrialWT SET SINGLE_USER WITH ROLLBACK IMMEDIATE;
GO

-- 删除数据库
DROP DATABASE WitchTrialWT;
GO

-- 重新创建
CREATE DATABASE WitchTrialWT;
GO
```

然后重新执行建表和初始化脚本。

## 导出数据库脚本

如果你需要导出数据库脚本给小组成员：

### 方法一：使用 SSMS 导出

1. 右键点击数据库 `WitchTrialWT`
2. 选择"任务" → "生成脚本"
3. 选择要导出的对象（表、视图、存储过程等）
4. 选择"高级"，设置：
   - 要编写脚本的数据的类型：架构和数据
   - 编写 DROP 和 CREATE 脚本：CREATE
5. 保存为 `.sql` 文件

### 方法二：使用命令行

```bash
# 导出架构
sqlcmd -S . -d WitchTrialWT -E -o schema.sql -Q "SELECT * FROM INFORMATION_SCHEMA.TABLES"

# 或使用 SQL Server Management Studio 的"生成脚本"功能
```

## 备份和恢复

### 备份数据库

```sql
BACKUP DATABASE WitchTrialWT 
TO DISK = 'C:\Backup\WitchTrialWT.bak'
WITH FORMAT, INIT, NAME = 'Full Backup of WitchTrialWT';
```

### 恢复数据库

```sql
RESTORE DATABASE WitchTrialWT 
FROM DISK = 'C:\Backup\WitchTrialWT.bak'
WITH REPLACE;
```

## 小组协作建议

1. **统一数据库版本**：
   - 使用相同的 SQL Server 版本
   - 使用相同的数据库脚本

2. **定期同步数据**：
   - 定期导出数据库脚本
   - 提交到 Git 仓库（放在 `Data/` 文件夹）

3. **测试数据管理**：
   - 准备一套标准的测试数据
   - 每个人使用相同的测试数据

4. **数据库命名规范**：
   - 表名使用 PascalCase（如 `Witch`）
   - 列名使用 PascalCase（如 `WitchID`）
   - 外键使用 `FK_` 前缀
   - 索引使用 `IX_` 前缀

## 需要帮助？

如果遇到数据库问题：

1. 检查 SQL Server 服务是否启动
2. 检查连接字符串是否正确
3. 检查数据库和架构是否存在
4. 查看错误日志获取详细信息
