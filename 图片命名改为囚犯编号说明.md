# 图片命名改为囚犯编号说明

## 📋 改动概述

将魔女头像图片的命名方式从**姓名拼音**改为**囚犯编号**，以支持多岛屿、多批次的魔女管理。

## 🎯 改动原因

### 原有方案的问题
- ❌ 使用姓名命名（如 `ema.png`, `hiro.png`）
- ❌ 需要维护 `囚犯编号 → 姓名 → 文件名` 的映射关系
- ❌ 不同批次可能出现重名，导致文件名冲突
- ❌ 扩展性差，每次新增魔女都要手动维护映射

### 新方案的优势
- ✅ 使用囚犯编号命名（如 `658.png`, `659.png`）
- ✅ 囚犯编号全局唯一，绝对不会冲突
- ✅ 零映射维护，数据库直接存储路径
- ✅ 符合现实逻辑（监狱系统用编号管理）
- ✅ 完美支持多岛屿、多批次扩展

## 📝 执行步骤

### 1. 图片文件重命名
使用脚本 `rename_images_to_prisoner_no.ps1` 批量重命名：

```powershell
powershell -ExecutionPolicy Bypass -File rename_images_to_prisoner_no.ps1
```

**重命名映射：**
| 原文件名 | 新文件名 | 囚犯编号 | 姓名 |
|---------|---------|---------|------|
| ema.png | 658.png | 658 | 樱羽艾玛 |
| hiro.png | 659.png | 659 | 二阶堂希罗 |
| anan.png | 660.png | 660 | 夏目安安 |
| noah.png | 661.png | 661 | 城崎诺亚 |
| leia.png | 662.png | 662 | 莲见蕾娅 |
| miria.png | 663.png | 663 | 佐伯米莉亚 |
| margo.png | 664.png | 664 | 宝生玛格 |
| nanoka.png | 665.png | 665 | 黑部奈叶香 |
| alisa.png | 666.png | 666 | 紫藤爱丽莎 |
| sherry.png | 667.png | 667 | 橘雪莉 |
| hanna.png | 668.png | 668 | 远野汉娜 |
| coco.png | 669.png | 669 | 泽渡可可 |
| meruru.png | 670.png | 670 | 冰上梅露露 |

**执行结果：**
- ✅ 成功重命名 13 个文件
- ✅ 自动创建备份：`Images_backup_20251126_000818`

### 2. 数据库更新
使用脚本 `update_avatar_path_to_prisoner_no.sql` 更新数据库：

```sql
UPDATE wt.Witch
SET AvatarPath = 'Images/' + PrisonerNo + '.png'
WHERE PrisonerNo IS NOT NULL;
```

**更新结果：**
- ✅ 13 条记录已更新
- ✅ 所有路径格式：`Images/658.png`, `Images/659.png` ...

### 3. 初始化脚本更新
修改了 `数据库完整初始化脚本_步骤1_基础结构.sql`：

**修改前（硬编码映射）：**
```sql
UPDATE w SET AvatarPath =
 CASE PrisonerNo
  WHEN '658' THEN 'Images/ema.png'
  WHEN '659' THEN 'Images/hiro.png'
  ...
 END
FROM wt.Witch w
WHERE AvatarPath IS NULL;
```

**修改后（动态生成）：**
```sql
UPDATE wt.Witch
SET AvatarPath = 'Images/' + PrisonerNo + '.png'
WHERE PrisonerNo IS NOT NULL AND AvatarPath IS NULL;
```

## 🔍 代码验证

### 图片加载逻辑（无需修改）
现有代码已经是动态读取路径，无需任何修改：

```csharp
// Form1.cs, WitchDetailForm.cs 等
string avatarPath = r["AvatarPath"].ToString();  // 从数据库读取
string fullPath = Path.Combine(AppContext.BaseDirectory, avatarPath);
if (File.Exists(fullPath))
{
    _avatar.Image = Image.FromFile(fullPath);
}
```

## 📦 备份信息

### 图片备份
- 位置：`Images_backup_20251126_000818/`
- 内容：所有原始图片文件（姓名命名）
- 用途：如需回滚，手动复制回 `Images/` 目录

### 数据库备份
脚本中创建了临时备份表 `#WitchAvatarBackup`，包含：
- WitchID
- PrisonerNo
- Name
- AvatarPath（旧值）

## 🚀 后续使用指南

### 新增魔女时
1. 准备图片文件，命名为囚犯编号：`671.png`, `672.png` ...
2. 将图片放入 `Images/` 目录
3. 导入魔女数据时，设置 `AvatarPath = 'Images/' + PrisonerNo + '.png'`
4. 无需任何映射配置

### 批量导入示例
```sql
-- 导入批次2的魔女
INSERT INTO wt.Witch (Name, PrisonerNo, AvatarPath, IslandID, BatchID)
VALUES 
    (N'新魔女1', '671', 'Images/671.png', 1, 2),
    (N'新魔女2', '672', 'Images/672.png', 1, 2),
    ...
```

## ✅ 验证清单

- [x] 图片文件已重命名（13个文件）
- [x] 数据库路径已更新（13条记录）
- [x] 初始化脚本已更新
- [x] 备份已创建
- [x] 代码无需修改（已验证）
- [ ] 应用程序测试（待用户测试）

## 📌 注意事项

1. **占位图不变**：`_placeholder.png` 保持原名
2. **其他图片不变**：`Jailer.png`, `warden.png` 等系统图片保持原名
3. **向后兼容**：如果数据库中有旧路径，代码仍能正常工作
4. **批次隔离**：后续实现时，通过 `BatchID` 过滤魔女，图片自动对应

## 🎉 改动完成

现在系统已经完全切换到囚犯编号命名方案，可以开始准备批次2的数据导入了！
