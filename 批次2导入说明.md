# 批次2导入说明

## 📋 概述

批次2包含13位新魔女，囚犯编号671-683。本次导入实现了**完全的批次隔离**，批次1和批次2的魔女互不干扰。

## 🎯 批次信息

- **岛屿**：魔女岛·壹（IslandID = 1）
- **批次**：批次2（BatchID = 2）
- **魔女数量**：13位
- **囚犯编号**：671-683
- **账号命名**：使用囚犯编号（671-683）
- **统一密码**：123456

## 👥 魔女名单

| 序号 | 囚犯编号 | 姓名 | 魔法 | 特点 |
|------|---------|------|------|------|
| 16 | 671 | 小鸟游六花 | 邪王真眼、永恒之暗 | 中二病少女，右眼眼罩 |
| 17 | 672 | 富樫勇太 | 漆黑烈焰使 | 隐藏中二病的普通人 |
| 18 | 673 | 丹生谷森夏 | 森夏魔女 | 前中二病，现充领袖 |
| 19 | 674 | 五月七日茴香 | 茴香魔女 | 占卜师，温和敏锐 |
| 20 | 675 | 凸守早苗 | 雷之征服者 | 元气满满，六花随从 |
| 21 | 676 | 七宫智音 | 智音魔女 | 音乐魔法，勇太青梅竹马 |
| 22 | 677 | 伊雷娜 | 灰之魔女 | 15岁天才魔女，旅行者 |
| 23 | 678 | 维多利加 | 高阶全属性魔法 | 伊雷娜之母，传奇魔女 |
| 24 | 679 | 沙耶 | 扫帚魔女 | 伊雷娜挚友，团队协作 |
| 25 | 680 | 芙兰 | 星辰魔女 | 伊雷娜师父，星空观测 |
| 26 | 681 | 席拉 | 暗夜魔女 | 芙兰师妹，暗影操控 |
| 27 | 682 | 琪琪 | 飞行魔法 | 魔女宅急便，13岁创业 |
| 28 | 683 | 冰上梅露露 | 治愈・再生 | 神秘魔女，渴望认可 |

## 📁 导入脚本

### 分步执行（推荐）

1. **batch2_step1_create_batch.sql** - 创建批次2
2. **batch2_step2_import_witches_basic.sql** - 导入基本信息
3. **batch2_step3_import_details_part1.sql** - 导入详细信息（671-675）
4. **batch2_step3_import_details_part2.sql** - 导入详细信息（676-680）
5. **batch2_step3_import_details_part3.sql** - 导入详细信息（681-683）
6. **batch2_step4_create_users.sql** - 创建用户账号

### 一键执行

```bash
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_import_all.sql
```

## 🔧 执行步骤

### 1. 执行导入脚本

```bash
# 方式1：分步执行
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_step1_create_batch.sql
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_step2_import_witches_basic.sql
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_step3_import_details_part1.sql
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_step3_import_details_part2.sql
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_step3_import_details_part3.sql
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_step4_create_users.sql

# 方式2：一键执行（推荐）
sqlcmd -S localhost -d WitchTrialWT -E -i batch2_import_all.sql
```

### 2. 设置用户密码

所有用户的密码需要设置为 `123456`。可以通过应用程序的密码设置功能完成。

### 3. 准备头像图片

将13位魔女的头像图片放入 `Images/` 目录，命名规则：

```
Images/671.png  - 小鸟游六花
Images/672.png  - 富樫勇太
Images/673.png  - 丹生谷森夏
Images/674.png  - 五月七日茴香
Images/675.png  - 凸守早苗
Images/676.png  - 七宫智音
Images/677.png  - 伊雷娜
Images/678.png  - 维多利加
Images/679.png  - 沙耶
Images/680.png  - 芙兰
Images/681.png  - 席拉
Images/682.png  - 琪琪
Images/683.png  - 冰上梅露露
```

## 🔒 批次隔离

### 数据库层面

批次2的所有数据都标记了 `BatchID = 2`：

```sql
-- 魔女表
SELECT * FROM wt.Witch WHERE BatchID = 2;

-- 用户表
SELECT * FROM wt.[User] WHERE BatchID = 2;
```

### 应用层面（待实现）

需要修改以下功能，添加批次过滤：

#### 1. 魔女图鉴（PokedexForm）

```csharp
// 只显示当前用户所在批次的魔女
var dt = _dal.GetWitches(islandId: currentIslandId, batchId: currentBatchId);
```

#### 2. 五子棋对弈（GomokuModeForm）

```csharp
// 只能与同批次的魔女对弈
var opponents = _dal.GetWitches(batchId: currentBatchId);
```

#### 3. 管理面板（Form1）

```csharp
// 管理员可以选择批次查看
// 普通用户只能看到自己批次
if (isAdmin)
{
    // 显示批次选择器
}
else
{
    // 自动过滤到当前用户批次
}
```

## 📊 验证清单

### 数据库验证

```sql
-- 1. 验证批次创建
SELECT * FROM wt.Batch WHERE BatchID = 2;

-- 2. 验证魔女数量
SELECT COUNT(*) FROM wt.Witch WHERE BatchID = 2;
-- 应该返回：13

-- 3. 验证用户数量
SELECT COUNT(*) FROM wt.[User] WHERE BatchID = 2;
-- 应该返回：13

-- 4. 验证用户-魔女关联
SELECT 
    u.Username,
    w.Name,
    w.PrisonerNo
FROM wt.[User] u
JOIN wt.UserWitch uw ON u.UserID = uw.UserID
JOIN wt.Witch w ON uw.WitchID = w.WitchID
WHERE u.BatchID = 2
ORDER BY u.Username;
-- 应该返回：13条记录

-- 5. 验证批次隔离
SELECT 
    BatchID,
    COUNT(*) AS WitchCount
FROM wt.Witch
GROUP BY BatchID;
-- 应该显示：
-- BatchID 1: 13位魔女
-- BatchID 2: 13位魔女
```

### 应用程序验证

- [ ] 使用批次2账号登录（如：671，密码：123456）
- [ ] 魔女图鉴只显示批次2的13位魔女
- [ ] 五子棋对弈只能选择批次2的魔女
- [ ] 无法看到批次1的魔女信息

## 🎨 头像图片建议

### 图片规格
- 格式：PNG（推荐）或 JPG
- 尺寸：建议 160x160 或更大（会自动缩放）
- 命名：囚犯编号.png（如：671.png）

### 占位图
如果某个魔女暂时没有头像，系统会自动使用占位图：
- `Images/_placeholder.png`

## 🚀 后续开发

### 1. 批次隔离功能

需要修改的文件：
- `DAL/WitchDAL.cs` - 添加批次过滤参数
- `UI/PokedexForm.cs` - 魔女图鉴批次过滤
- `UI/GomokuModeForm.cs` - 五子棋对手批次过滤
- `Form1.cs` - 管理面板批次选择

### 2. 批次管理功能

可以考虑添加：
- 批次切换界面
- 批次统计信息
- 跨批次数据对比
- 批次导出功能

### 3. 多岛屿支持

如果未来需要添加新岛屿：
1. 在 `wt.Island` 表中添加新岛屿
2. 为新岛屿创建批次
3. 导入新岛屿的魔女数据
4. 创建对应的用户账号

## 📝 注意事项

1. **批次编号**：批次2的 BatchID 是自动生成的，通常是2，但请以实际数据库为准
2. **账号命名**：批次2使用囚犯编号作为账号名，与批次1的姓名命名不同
3. **密码安全**：生产环境应使用更安全的密码策略
4. **图片路径**：确保图片文件存在，否则会显示占位图
5. **批次隔离**：应用层的批次隔离功能需要单独开发

## ✅ 完成标志

- [x] 批次2创建完成
- [x] 13位魔女基本信息导入
- [x] 13位魔女详细信息导入
- [x] 13个用户账号创建
- [x] 用户-魔女关联建立
- [ ] 用户密码设置（需要应用程序）
- [ ] 头像图片准备（需要用户提供）
- [ ] 批次隔离功能开发（需要修改代码）

## 🎉 总结

批次2的数据导入已经完成，包括13位魔女和13个用户账号。现在可以开始准备头像图片，并开发批次隔离功能，确保批次1和批次2的魔女完全独立运行。
